# apipost-rs 优化改进方案

## 第一阶段：架构重构（优先级最高）

### 1.1 拆分 main_view.rs 为组件化结构

当前 `src/ui/main_view.rs` 3584行，包含全部UI逻辑。拆分为：

```
src/ui/
  main_view.rs          → 布局编排 (~200行)
  sidebar/
    mod.rs              → 侧边栏容器
    history_panel.rs    → 历史面板
    collection_panel.rs → 收藏面板
    environment_panel.rs→ 环境面板
  request/
    mod.rs              → 请求构造器容器
    url_bar.rs          → URL栏+方法选择+发送按钮
    params_panel.rs     → 参数面板
    headers_panel.rs    → 请求头面板
    body_panel.rs       → 请求体面板
    auth_panel.rs       → 认证面板
    settings_panel.rs   → 设置面板
  response/
    mod.rs              → 响应查看器
    body_viewer.rs      → 响应体视图
    headers_viewer.rs   → 响应头视图
    cookies_viewer.rs   → Cookie视图
  dialogs/
    mod.rs              → 对话框
    settings_dialog.rs  → 设置对话框
    code_gen_dialog.rs  → 代码生成对话框
```

### 1.2 修复异步请求，消除UI阻塞

当前在 main_view.rs:571-572 使用同步阻塞：
```rust
let runtime = tokio::runtime::Runtime::new().unwrap();
let result = runtime.block_on(self.app_state.send_request(request));
```

改为使用 gpui 的异步机制，通过 `cx.spawn()` 在后台执行，`cx.notify()` 更新UI。

### 1.3 修复主题系统

`themes/mod.rs` 定义了完整 Theme 结构体，但 main_view.rs 中几乎所有颜色硬编码为深色值。
将所有硬编码 rgb() 替换为 Theme 结构体字段引用，确保主题切换实时生效。

## 第二阶段：功能补全（优先级中）

### 2.1 实现收藏夹功能
- 显示数据库中已保存的请求列表
- 支持文件夹管理
- 支持右键菜单操作

### 2.2 实现环境管理面板
- 显示所有环境列表
- 支持添加/编辑/删除环境变量
- 支持环境切换和JSON导入导出

### 2.3 实现脚本引擎
- 集成 JavaScript 引擎
- 预请求脚本执行
- 测试脚本断言

### 2.4 添加 WebSocket 支持
- WebSocket 连接管理
- 消息发送/接收展示

### 2.5 实现快捷键系统
- Ctrl+Enter 发送请求
- Ctrl+N 新建标签
- Ctrl+W 关闭标签

## 第三阶段：体验优化（优先级低）

### 3.1 UI/UX改进
- Toast 通知系统
- 动画效果
- 拖拽排序
- 虚拟滚动

### 3.2 高级功能
- 请求链接（提取响应值）
- 响应 Diff 对比
- 批量请求运行器
- Postman 格式兼容导入导出
- GraphQL 支持
